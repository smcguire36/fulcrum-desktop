import {PersistentObject} from 'minidb';

export default class Base extends PersistentObject {
  // static register(modelClass) {
  //   PersistentObject.includeInto(modelClass);
  //   PersistentObject.register(modelClass);
  // }
}
